<%- include('layout') %>
<h2><%= t.notFoundTitle || 'الصفحة غير موجودة' %></h2>
<p><%= t.notFoundMsg || 'عذراً، الصفحة التي تبحث عنها غير موجودة.' %></p>
<a href="/" class="btn btn-primary"><%= t.back || 'العودة للرئيسية' %></a>
